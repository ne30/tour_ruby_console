require '../tours'

RSpec.describe Tour do
    # it 'Proper intialise of the tour class' do
    #     var = Tour.new("tour_code", "from", "to", "day", "start_time", "end_time", 10, ["passenger"], ["solo_male_passenger"], ["solo_female_passenger"])
    #     expect (var.from).to eql?("tour_code")
    # end
end